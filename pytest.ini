[pytest]
# only this project's tests: a packaged Eki.app in the working copy carries a
# whole Python distribution, and pytest will happily try to collect it
testpaths = tests
norecursedirs = .venv Eki.app mac/build dist .git
asyncio_mode = strict
markers =
    real_processes: checks real processes on this Mac (lsof, ps)
